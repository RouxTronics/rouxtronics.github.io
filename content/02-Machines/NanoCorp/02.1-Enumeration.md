## DNS Configure 

> Edit `/etc/hosts`

```sh
nxc smb $TARGET --generate-hosts-file hosts
cat hosts /etc/hosts | sudo tee /etc/hosts | head -1
```

![](<./attachments/02-Enumeration.png>)

- Clock Skew configure
> Using [ntpdate](<../../../../../../02-Capture/ntpdate.md>)
```
sudo ntpdate dc01.nanocorp.htb
```

## SMB (445)

```sh
nxc smb dc01.nanocorp.htb -u '' -p ''
nxc smb dc01.nanocorp.htb -u guest -p ''
nxc smb dc01.nanocorp.htb -u RouxTronics -p ''
```

![](<./attachments/02-Enumeration-1.png>)
- Need Creds 

## LDAP (389)

```sh
nxc ldap dc01.nanocorp.htb -u RouxTronics -p ''
```

![](<./attachments/02-Enumeration-2.png>)

- `signing:None` - the DC does not require LDAP signing. Unsigned LDAP binds are accepted.
- `channel binding:No TLS cert` - there’s no LDAPS channel-binding (EPA) enforcement either.

Together, those mean the DC is vulnerable to NTLM relay to LDAP. With signing not required and channel binding not enforced, an attacker who can coerce or capture an authentication can relay that NTLM auth straight to LDAP and have it accepted.

> was [patched](https://app.hackthebox.com/machines/NanoCorp?tab=changelog) 

## nanocorp.htb - HTTP (80)

### Site 

The site is for a cyber security company


![](<./attachments/Pasted image 20260623151143.png>)

Each of the four squares pop up information. 

#### About Section 

> [!INFO]
> Found Sub-Domain 
> The “About Us” one has a link to `hire.nanocorp.htb`:

![](<./attachments/02-Enumeration-4.png>)

- Add to `/etc/hosts` file 

![](<./attachments/02-Enumeration-5.png>)

#### Contact Section

- Submitting the form sends a POST to `/index.html`, which suggests it does nothing.
 
> Used [[Burp Suite]]

### Tech Stack

This feels like a static site. The HTTP response headers show just Apache:

```sh
curl http://nanocorp.htb
```

![](<./attachments/02-Enumeration-7.png>)

The main page loads as both `/` and `/index.html`.
#### 404 Page 
The 404 page is the [default Apache 404](https://0xdf.gitlab.io/cheatsheets/404#apache--httpd), but it does show that PHP is installed and running:


![](<./attachments/02-Enumeration-6.png>)

likely the [XAMPP for Windows stack](https://www.apachefriends.org/download.html).
### Directory Brute Force

> Using: [Feroxbuster](<../../../../../../02-Capture/Feroxbuster.md>)

- include `-x html,php` as there are `.html` pages, and the headers indicate that PHP could be enabled.
- lowercase wordlist as Windows typically isn’t case sensitive:

```sh
 feroxbuster -u http://nanocorp.htb -x html,php -w /usr/share/wordlists/seclists/Discovery/Web-Content/raft-medium-directories-lowercase.txt --dont-extract-links
```

![](<./attachments/02-Enumeration-8.png>)

- `/examples` is returning 503, which suggests it’s being proxied to something that is dead.
- `/server-status` and `/server-info` are standard Apache pages (`mod_status` and `mod_info`) that are typically blocked when not coming from localhost.

|Path|Response Code|
|---|---|
|`/xampp`|404|
|`/security`|404|
|`/licenses`|403|
|`/phpmyadmin`|403|
|`/webalizer`|403|
|`/server-status`|403|
|`/server-info`|403|

## hire.nanocorp.htb

### Site 
The site presents a form to apply for a position:

![](<./attachments/02-Enumeration-9.png>)


### Tech Stack

![](<./attachments/02-Enumeration-10.png>)

### 404 

![](<./attachments/02-Enumeration-11.png>)


### Directory Brute Force

```sh
feroxbuster -u http://hire.nanocorp.htb -x php,html -w /usr/share/wordlists/seclists/Discovery/Web-Content/raft-medium-directories-lowercase.txt  --dont-extract-links
```

![](<./attachments/02-Enumeration-12.png>)

### File upload 

`shell.php`
```php
<?php echo("Succuss"); ?>
```

![](<./attachments/02.1-Enumeration-1.png>)

> Need to Be `zip` file 

```sh
 zip exploit.php.zip exploit.php
```

### Img Information

- Download server image 
```sh
wget http://hire.nanocorp.htb/images/nanocorp.jpg
```

- Read image
```sh
exiftool nanocorp.jpg
```

![](<./attachments/02.1-Enumeration.png>)

> image upload to server 2025 Apr 02
> 